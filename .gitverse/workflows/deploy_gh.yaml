name: deploy_gh
on:
  workflow_call:

jobs:
  deploy-gh:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on:
      ubuntu-latest
    steps:
      - name: deploy_to_pages
        id: deployment
        uses: actions/deploy-pages@v4


